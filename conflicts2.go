package main

/*func (dp *depPool) checkInnerConflicts() {
	conflicts := make([]string)

	for pkg := range dp.Repo {
		for pkg.Conflicts().ForEach(func(conflict string) error {
			if dp.hasSatisfier(conflict) {

			}

			return nil
		})
	}
}*/
